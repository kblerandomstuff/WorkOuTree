<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ovh.krs.madgp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MADGP">
        <activity android:name=".wo.wo1activity" />
        <activity android:name=".wo.wo2activity" />
        <activity android:name=".wo.wo3activity" />
        <activity
            android:name=".MainActivity"
            android:configChanges="locale|orientation"
            android:label="@string/app_name"
            android:theme="@style/Theme.MADGP.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>